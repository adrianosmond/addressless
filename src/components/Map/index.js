import React, { Component } from 'react';
import options from './options.json';

class Map extends Component {
  render () {
    console.log(options);
    return (
      <div>...</div>
    );
  }
}

export default Map;
